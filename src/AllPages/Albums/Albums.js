import React from 'react';

import AlBums from "../../components/Albums/AlBums";

const Albums = () => {
    return (
        <div>
            <AlBums/>
        </div>
    );
};

export default Albums;